{
  "$schema": "https://railpack.com/schema.json",
  "build": {
    "installCommand": "cd backend && npm install --include=optional && npm install --os=linux --cpu=x64 sharp",
    "buildCommand": "cd backend && npm run build"
  },
  "deploy": {
    "startCommand": "cd backend && npm install --os=linux --cpu=x64 sharp && npm run start"
  }
}