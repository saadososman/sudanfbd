{
  "name": "sudanese-forum-platform",
  "private": true,

  "engines": {
    "node": "20.x"
  },

  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "dev:frontend": "npm --prefix frontend run dev",
    "dev:backend": "npm --prefix backend run develop"
  },

  "workspaces": [
    "frontend",
    "backend"
  ]
}